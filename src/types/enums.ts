export enum BookingSteps {
  calendar = 'calendar',
  form = 'form',
  time = 'time',
  confirmation = 'confirmation',
}
export enum InputType {
  text = 'text',
  email = 'email',
  password = 'password',
  number = 'number',
  date = 'date',
  time = 'time',
  select = 'select',
  radio = 'radio',
  checkbox = 'checkbox',
  textarea = 'textarea',
}
